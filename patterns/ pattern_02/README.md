# Right-Angled Triangle Pattern in Java

This Java program prints a **right-angled triangle** using nested loops.  
It helps beginners practice **loops, nested loops, and logic building** with pattern problems.

---

## 📌 Pattern Description

- **Rows (`n`)**: Number of rows in the triangle  
- **Logic**: Each row `i` contains `i` stars (`*`).  
- The first row has 1 star, the second row has 2 stars, and so on until the `n`th row.

